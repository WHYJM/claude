# Folder: apps/web/src/components/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

React UI components: application-specific and shared UI primitives.
React UI 组件：应用专用组件和共享 UI 基础组件。
Boundary: Receives props; no direct API calls; uses hooks for state/effects.
边界：接收 props；不直接调用 API；使用 hooks 获取状态/副作用。
Invariant: UI components are presentational; logic delegated to parent or hooks.
不变量：UI 组件是展示型的；逻辑委托给父组件或 hooks。

## Files

- `RecipeForm.tsx`: Form component for creating/editing recipes / 创建/编辑食谱的表单组件
- `ui/`: Shadcn/ui base components library / Shadcn/ui 基础组件库
