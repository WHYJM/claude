# Folder: apps/web/src/components/ui/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Shadcn/ui component library - accessible, customizable UI primitives.
Shadcn/ui 组件库 - 可访问、可定制的 UI 基础组件。
Boundary: Pure UI components; no business logic; styled with Tailwind CSS.
边界：纯 UI 组件；无业务逻辑；使用 Tailwind CSS 样式。
Invariant: Components follow Radix UI patterns with consistent API.
不变量：组件遵循 Radix UI 模式，API 一致。

## Files

- `badge.tsx`: Badge component for labels/tags / 徽章组件，用于标签
- `button.tsx`: Button component with variants / 按钮组件（含多种变体）
- `card.tsx`: Card container component / 卡片容器组件
- `dialog.tsx`: Modal dialog component / 模态对话框组件
- `input.tsx`: Text input component / 文本输入组件
- `label.tsx`: Form label component / 表单标签组件
- `scroll-area.tsx`: Custom scrollable area / 自定义滚动区域
- `select.tsx`: Select dropdown component / 下拉选择组件
- `separator.tsx`: Visual separator line / 视觉分隔线
- `tabs.tsx`: Tab navigation component / 标签页导航组件
- `textarea.tsx`: Multi-line text input / 多行文本输入组件
