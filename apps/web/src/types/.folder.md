# Folder: apps/web/src/types/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

TypeScript type definitions specific to web application.
Web 应用专用的 TypeScript 类型定义。
Boundary: Defines local types; shared types should be in packages/shared-types.
边界：定义本地类型；共享类型应放在 packages/shared-types。
Invariant: Types are exported and match API response shapes.
不变量：类型被导出且匹配 API 响应结构。

## Files

- `auth.ts`: Authentication-related types (User, Session) / 认证相关类型（User, Session）
- `recipe.ts`: Recipe domain types (Recipe, Ingredient) / 食谱领域类型（Recipe, Ingredient）
