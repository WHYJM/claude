# Folder: apps/web/src/lib/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Utility functions and helpers for the web application.
Web 应用的工具函数和辅助函数。
Boundary: Pure functions; no side effects; no React dependencies.
边界：纯函数；无副作用；无 React 依赖。
Invariant: Utilities are stateless and testable in isolation.
不变量：工具函数无状态，可独立测试。

## Files

- `utils.ts`: General utility functions (cn for classnames) / 通用工具函数（cn 用于类名合并）
