# Folder: apps/web/src/hooks/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Custom React hooks for reusable stateful logic.
可复用有状态逻辑的自定义 React 钩子。
Boundary: Encapsulates side effects and state; used by route components.
边界：封装副作用和状态；被路由组件使用。
Invariant: Hooks are composable; each hook has single responsibility.
不变量：钩子可组合；每个钩子单一职责。

## Files

- `useAuth.ts`: Authentication state and actions hook / 认证状态和操作钩子
- `useCollaboration.ts`: Real-time collaboration WebSocket hook / 实时协作 WebSocket 钩子
- `useKeyboard.ts`: Mobile keyboard handling hook / 移动端键盘处理钩子
- `README.md`: Documentation for mobile keyboard adaptation / 移动端键盘适配文档
