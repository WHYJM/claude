# Folder: apps/mobile/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

React Native mobile application using Expo framework.
使用 Expo 框架的 React Native 移动应用。
Boundary: Shares types with packages/*; calls gateway API.
边界：与 packages/* 共享类型；调用 gateway API。
Invariant: Uses Expo Router for file-based navigation.
不变量：使用 Expo Router 进行基于文件的导航。

## Files

- `app/`: Expo Router file-based routes / Expo Router 基于文件的路由
- `app.json`: Expo configuration / Expo 配置
- `package.json`: Package dependencies and scripts / 包依赖和脚本
- `tsconfig.json`: TypeScript configuration / TypeScript 配置
- `expo-env.d.ts`: Expo TypeScript environment types / Expo TypeScript 环境类型
- `.expo/`: Expo cache and generated files / Expo 缓存和生成文件
