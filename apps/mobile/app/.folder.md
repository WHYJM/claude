# Folder: apps/mobile/app/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Expo Router file-based routes and screens for mobile app.
Expo Router 基于文件的路由和移动应用屏幕。
Boundary: Each file is a route/screen; uses React Native components.
边界：每个文件是一个路由/屏幕；使用 React Native 组件。
Invariant: File names match URL paths; folders create nested routes.
不变量：文件名匹配 URL 路径；文件夹创建嵌套路由。

## Files

- `_layout.tsx`: Root layout component (navigation structure) / 根布局组件（导航结构）
- `index.tsx`: Home screen (/) / 首页屏幕
- `recipes/`: Recipes section routes / 食谱区路由
- `fridge/`: Fridge inventory routes / 冰箱库存路由
