# Folder: apps/gateway/src/db/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Database layer using Drizzle ORM with PostgreSQL.
使用 Drizzle ORM 和 PostgreSQL 的数据库层。
Boundary: Exports db client and schema; used by routes and auth.
边界：导出 db 客户端和 schema；被路由和认证使用。
Invariant: Schema matches PostgreSQL tables; migrations in drizzle/ folder.
不变量：Schema 匹配 PostgreSQL 表；迁移在 drizzle/ 文件夹。

## Files

- `index.ts`: Database client initialization / 数据库客户端初始化
- `schema.ts`: Drizzle ORM table schema definitions / Drizzle ORM 表 schema 定义
