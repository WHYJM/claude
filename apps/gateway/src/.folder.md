# Folder: apps/gateway/src/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Gateway source code: routes, middleware, database, and configuration.
网关源代码：路由、中间件、数据库和配置。
Boundary: Entry is index.ts; routes handle API logic; middleware handles auth.
边界：入口为 index.ts；routes 处理 API 逻辑；middleware 处理认证。
Invariant: All routes are protected by auth middleware unless explicitly public.
不变量：所有路由受认证中间件保护，除非显式标记为公开。

## Files

- `index.ts`: Application entry point, Hono app setup / 应用入口，Hono 应用配置
- `routes/`: API route handlers / API 路由处理器
- `middleware/`: Request middleware (auth, etc.) / 请求中间件（认证等）
- `lib/`: Shared utilities and configuration / 共享工具和配置
- `db/`: Database connection and schema / 数据库连接和 schema
