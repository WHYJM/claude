# Folder: apps/gateway/src/lib/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Shared utilities and configuration for gateway.
网关的共享工具和配置。
Boundary: Provides configuration objects; no side effects on import.
边界：提供配置对象；导入时无副作用。
Invariant: Environment variables are validated at startup.
不变量：环境变量在启动时验证。

## Files

- `auth.ts`: Better Auth configuration and instance / Better Auth 配置和实例
- `env.ts`: Environment variable definitions and validation / 环境变量定义和验证
