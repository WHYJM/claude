# Folder: apps/gateway/src/middleware/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Hono middleware for request processing.
用于请求处理的 Hono 中间件。
Boundary: Intercepts requests before route handlers; modifies context.
边界：在路由处理器之前拦截请求；修改上下文。
Invariant: Middleware is composable and order-dependent.
不变量：中间件可组合，执行顺序敏感。

## Files

- `auth.ts`: Authentication middleware (session validation) / 认证中间件（会话验证）
