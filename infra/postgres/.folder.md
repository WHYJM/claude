# Folder: infra/postgres/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

PostgreSQL database initialization scripts.
PostgreSQL 数据库初始化脚本。
Boundary: Runs on container startup; creates schema and extensions.
边界：容器启动时运行；创建 schema 和扩展。
Invariant: Scripts are idempotent; includes pgvector extension setup.
不变量：脚本幂等；包含 pgvector 扩展配置。

## Files

- `init.sql`: Database initialization SQL (schema, extensions, seed data) / 数据库初始化 SQL（schema、扩展、种子数据）
