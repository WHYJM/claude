# Folder: services/core-java/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Core business logic service using Spring Boot 3 + Java 21.
使用 Spring Boot 3 + Java 21 的核心业务逻辑服务。
Boundary: Receives requests from gateway; accesses PostgreSQL.
边界：接收来自 gateway 的请求；访问 PostgreSQL。
Invariant: RESTful API on port 8080; stateless request handling.
不变量：RESTful API 在端口 8080；无状态请求处理。

## Files

- `src/`: Java source code directory / Java 源代码目录
- `pom.xml`: Maven build configuration / Maven 构建配置
- `Dockerfile`: Container build configuration / 容器构建配置
