# Folder: services/ai-agent/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

AI Agent service using FastAPI + Google ADK + Gemini 2.0.
使用 FastAPI + Google ADK + Gemini 2.0 的 AI 代理服务。
Boundary: Receives requests from gateway; uses Gemini for AI features.
边界：接收来自 gateway 的请求；使用 Gemini 进行 AI 功能。
Invariant: REST API on port 8000; agent tools are modular.
不变量：REST API 在端口 8000；代理工具模块化。

## Files

- `main.py`: FastAPI application entry point / FastAPI 应用入口
- `smart_kitchen_agent/`: Google ADK agent package / Google ADK 代理包
- `adk.yaml`: Google ADK configuration / Google ADK 配置
- `requirements.txt`: Python dependencies / Python 依赖
- `Dockerfile`: Container build configuration / 容器构建配置
