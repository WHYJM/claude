# Folder: services/realtime-go/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Real-time collaboration service using Go + Gin + WebSocket.
使用 Go + Gin + WebSocket 的实时协作服务。
Boundary: Handles WebSocket connections; uses Redis pub/sub for messaging.
边界：处理 WebSocket 连接；使用 Redis pub/sub 进行消息传递。
Invariant: WebSocket API on port 8081; supports concurrent connections.
不变量：WebSocket API 在端口 8081；支持并发连接。

## Files

- `main.go`: Application entry point with all handlers / 应用入口（包含所有处理器）
- `go.mod`: Go module definition / Go 模块定义
- `Dockerfile`: Container build configuration / 容器构建配置
