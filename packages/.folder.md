# Folder: packages/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Shared libraries consumed by apps/, providing reusable code across frontend.
共享库，供 apps/ 消费，提供前端可复用代码。
Boundary: Exports types, components, and utilities; no direct backend calls.
边界：导出类型、组件和工具函数；不直接调用后端。
Invariant: All packages are TypeScript-first with strict type exports.
不变量：所有包以 TypeScript 优先，严格类型导出。

## Files

- `shared-types/`: Shared TypeScript type definitions / 共享 TypeScript 类型定义
- `api-client/`: Type-safe API client for gateway communication / 类型安全的 API 客户端
- `ui/`: Shared UI components library / 共享 UI 组件库
