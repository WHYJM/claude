# Folder: packages/ui/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Shared UI component library for frontend applications.
前端应用共享的 UI 组件库。
Boundary: Exports React components; no business logic; styled with Tailwind.
边界：导出 React 组件；无业务逻辑；使用 Tailwind 样式。
Invariant: Components are cross-platform compatible (web/mobile where possible).
不变量：组件跨平台兼容（尽可能支持 web/mobile）。

## Files

- `src/index.ts`: Component exports / 组件导出
- `package.json`: Package configuration / 包配置
- `tsconfig.json`: TypeScript configuration / TypeScript 配置
- `dist/`: Compiled output / 编译输出
