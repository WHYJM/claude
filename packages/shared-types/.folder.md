# Folder: packages/shared-types/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Shared TypeScript type definitions for all frontend applications.
所有前端应用共享的 TypeScript 类型定义。
Boundary: Exports types only; no runtime code; consumed by apps/*.
边界：仅导出类型；无运行时代码；被 apps/* 消费。
Invariant: Types match backend API response shapes.
不变量：类型匹配后端 API 响应结构。

## Files

- `src/index.ts`: Main export file for all shared types / 所有共享类型的主导出文件
- `package.json`: Package configuration / 包配置
- `tsconfig.json`: TypeScript configuration / TypeScript 配置
- `dist/`: Compiled output / 编译输出
