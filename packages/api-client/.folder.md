# Folder: packages/api-client/

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件：当本文件夹的结构/职责/文件列表变化时，更新此文档。

Type-safe API client for gateway communication.
用于 gateway 通信的类型安全 API 客户端。
Boundary: Wraps fetch/HTTP; exports typed methods; used by apps/*.
边界：封装 fetch/HTTP；导出类型化方法；被 apps/* 使用。
Invariant: All methods return typed responses matching shared-types.
不变量：所有方法返回匹配 shared-types 的类型化响应。

## Files

- `src/index.ts`: API client implementation and exports / API 客户端实现和导出
- `package.json`: Package configuration / 包配置
- `tsconfig.json`: TypeScript configuration / TypeScript 配置
- `dist/`: Compiled output / 编译输出
